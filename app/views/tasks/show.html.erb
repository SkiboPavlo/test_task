<div class="form-group">
  <div class="col-md-4">
    <p><strong>Title:</strong>
      <%= @task.title %></p>
    <p><strong>Description:</strong>
      <%= @task.description %></p>
    <p><strong>User_id:</strong>
      <%= @task.user_id %></p>
    <p><strong>Assignee_id:</strong>
      <%= @task.assignee_id %></p>
    <p><strong>Task_list_id:</strong>
      <%= @task.task_list_id %></p>

    <%= link_to 'Back', task_list_tasks_path(@task.task_list),class: 'btn btn-primary' %>
  </div>
</div>
