<div class="form-group">
  <div class="col-md-4">
    <p><strong>Title:</strong>
      <%= @task_list.title %></p>
    <p><strong>Project_id:</strong>
      <%= @task_list.project_id %></p>
      <p><strong>Project_title:</strong>
      <%= @project.title %></p>

    <%= link_to "New task", new_project_task_list_task_path(@project, @task_list),
      class: 'btn btn-primary' %>
    <%= link_to 'All Tasks', project_task_list_tasks_path(@project, @task_list), class: 'btn btn-primary' %>
    <%= link_to 'Back', project_task_lists_path, class: 'btn btn-default' %>
  </div>
</div>
